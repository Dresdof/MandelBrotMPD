global Mandelbrot
	
	type Complex = ptr ComplexRec;

	type ComplexRec = rec(
		double realPart;
		double imaginaryPart;
	);

	op complex(double realPart, double imaginaryPart) returns Complex c

	op mandelbrotIterate(val Complex complex, int maxIterations) returns int i
	
	op absolute(Complex complex) returns real r

body Mandelbrot separate
